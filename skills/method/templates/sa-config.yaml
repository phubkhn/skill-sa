# Optional Solution Architect preferences. Missing configuration never blocks a skill.

mode: brief                    # quick | brief | artifact
language: en
docs-root: docs/architecture
diagram: plantuml
contracts:
  sync: openapi-3.1
  async: asyncapi-2.6
